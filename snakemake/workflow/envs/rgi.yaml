name: rgi
channels:
  - conda-forge
  - bioconda
  # Block default channels to comply with Anaconda licensing
  - nodefaults
dependencies:
  - rgi=6.0.5
  # Needed for run output postprocessing
  - jq=1.8.1

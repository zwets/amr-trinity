name: resfinder
channels:
  - conda-forge
  - bioconda
  # Block default channels to comply with Anaconda licensing
  - nodefaults
dependencies:
  - resfinder=4.7.2
  # For installation of the database from GitHub
  - git
